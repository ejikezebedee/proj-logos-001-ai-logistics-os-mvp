# Server-only authentication and backend configuration.
API_MODE=mock
BACKEND_API_BASE_URL=http://localhost:3001

# Generate a unique value of at least 32 characters. Never commit the real value.
AUTH_SESSION_SECRET=
